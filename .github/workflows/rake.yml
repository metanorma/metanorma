# Auto-generated by Cimas: Do not edit it manually!
# See https://github.com/metanorma/cimas
name: rake

on:
  push:
    branches: [ master, main ]
    tags: [ v* ]
  pull_request:

jobs:
  rake:
    uses: metanorma/ci/.github/workflows/inkscape-rake.yml@rt-add-fontist-repo-action
    with:
      private-fonts: true
    secrets:
      pat_token: ${{ secrets.METANORMA_CI_PAT_TOKEN }}
